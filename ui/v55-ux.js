/* Browser-local utilities. Sandbox navigation is owned solely by v552-sandbox-flow.js. */
(function(V4){'use strict';
  const backupKeys=['op-generator-saved-v4','op-crew-fruit-continuity','op-v5-records','op-v5-living-worlds','op-v54-voyage-luck'];
  window.openSandboxPage=button=>{const panel=document.getElementById('sandbox-panel'),mount=document.getElementById('sandbox-mount');if(panel&&mount&&!mount.contains(panel))mount.appendChild(panel);if(panel)panel.open=true;window.switchTab?.('tab-sandbox',button);window.setSandboxStep?.(1);};
  window.exportAllLocalData=()=>{const data={schema:'one-piece-sandbox-backup',version:V4.build?.schemaVersion||'unknown',createdAt:new Date().toISOString(),localData:Object.fromEntries(backupKeys.map(key=>[key,localStorage.getItem(key)]).filter(([,value])=>value!==null))},blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`one-piece-sandbox-backup-${Date.now()}.json`;a.click();URL.revokeObjectURL(a.href);};
  window.importAllLocalData=file=>{if(!file)return;const reader=new FileReader();reader.onload=()=>{try{const data=JSON.parse(reader.result);if(data.schema!=='one-piece-sandbox-backup'||!data.localData||typeof data.localData!=='object')throw new Error('This is not a compatible One Piece Sandbox backup.');const saved=data.localData['op-generator-saved-v4'];if(saved!==undefined&&!Array.isArray(JSON.parse(saved)))throw new Error('Saved character data is invalid.');for(const key of backupKeys)if(Object.prototype.hasOwnProperty.call(data.localData,key))localStorage.setItem(key,String(data.localData[key]));alert('Backup imported. Reload the page to restore the complete roster.');}catch(error){alert(`Import failed: ${error.message}`);}};reader.readAsText(file);};
}(window.OnePieceRollV4));
