/* Weapon-grade scoring is database configuration, not inline generator content. */
(function (V4) {
  'use strict';
  V4.database.weaponGradeScores = Object.freeze({
    Standard: 30,
    Skillful: 55,
    Great: 78,
    Supreme: 92
  });
}(window.OnePieceRollV4));

