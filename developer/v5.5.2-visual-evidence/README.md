# V5.5.2 Visual Evidence

Browser screenshots have not been fabricated. This environment blocks browser automation for local `file:` URLs, so the real-match presentation checks and screenshot capture must be run from the application browser after opening `index.html`.

Use **Developer Tests → Run Real Battle Presentation Test**, then capture the requested telegraph, defense, impact, accessibility, Sandbox, Adventure, and Saved Character states here.
