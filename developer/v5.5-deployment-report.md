# V5.5 Deployment Report

Release entrypoints added: `index.html`, `404.html`, `.nojekyll`, and `assets/css/app.css`.

## HTTP checks

- `/onepiecerollv4.html`: HTTP 200
- `/OnePieceRollV4/`: HTTP 200 from a parent static host
- `/OnePieceRollV4/onepiecerollv4.html`: HTTP 200
- `/OnePieceRollV4/assets/css/app.css`: HTTP 200
- Script references: 86; missing local script files: 0
- Runtime asset URLs use relative paths; no `file://` or Windows absolute runtime path found.

Browser-local saves include an export/import schema (`one-piece-sandbox-backup`) whose schema version comes from `engine/build.js`. A user should export before clearing browser data or moving to another domain.

## V5.5.1 deployment acceptance checklist

| Criterion | Status | Evidence |
|---|---|---|
| `index.html` is the app entrypoint | PASS | `onepiecerollv4.html` is now a compatibility redirect. |
| Repository-subpath-safe runtime paths | PASS | Scripts and styles use relative URLs only. |
| Local source encoding | PASS | 127 scanned files, 0 findings. |
| GitHub Pages deployment | BLOCKED | This workspace has no Git repository or GitHub remote; no public URL can be recorded honestly. |
| Public browser-console result | BLOCKED | No public deployment exists to test. |

